import { color, length } from './styles'

export const card = {
  borderWidth: 0,
  backgroundColor: color.white,
  shadowColor: color.black,
  shadowOffset: {
    width: 0,
    height: 1
  },
  shadowRadius: 2,
  shadowOpacity: 0.1
}
